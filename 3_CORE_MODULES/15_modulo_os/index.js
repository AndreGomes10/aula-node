const os = require("os");

console.log(os.cpus());  // quantos cpu tem no serviços

console.log(os.freemem());  // quanto de memoria livre tem na maquina

console.log(os.homedir());  // qual o diretorio principal da maquina

console.log(os.type());  // qual o tipo de sistema operacional que esta rodando nessa maquina
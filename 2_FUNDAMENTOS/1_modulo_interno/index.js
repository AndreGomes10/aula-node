const meuModulo = require('./meu_modulo')  /* tem que passar o caminho relativo
                                              que ele esta dentro da pasta, pode usa com .js ou sem*/
const soma = meuModulo.soma

soma(2, 3)
soma(5, 10)